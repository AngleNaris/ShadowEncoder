# ShadowEncoder v2.2.4

- 修复「导出预设」点击后无任何反应的问题：WebView2 会拦截 HTML5 `<a download>` 下载，现改为调用原生保存对话框选择位置后写入 JSON 文件（取消则不保存）。

推荐下载 `ShadowEncoder_2.2.4_x64-setup.exe`；也提供 MSI。
安装包未签名，请对照 `SHA256SUMS.txt` 校验后运行。
